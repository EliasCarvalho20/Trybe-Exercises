const upperCase = (str) => str.toUpperCase();

const firstLetter = (str) => str[0];

const concatString = (str1, str2) => str1.concat(str2);

module.exports = { upperCase, firstLetter, concatString };
